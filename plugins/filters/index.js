import Vue from 'vue'
import {currency} from './currency';

const filtersArray = {currency};



// export default {
//   function (filtersVue.filter(key, filtersArray[key])
// }
//     // Object.keys(filtersArray).forEach((key) => {
//   //});

